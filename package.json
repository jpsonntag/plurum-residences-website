{
  "name": "plurum-residences-marketing",
  "version": "0.2.0",
  "private": true,
  "description": "Hugo static marketing website for Plurum Residences.",
  "scripts": {
    "build": "docker build --target exporter --output type=local,dest=public .",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "verify": "docker compose config && docker compose build"
  }
}
